//Deck implementation

#include <iostream>
#include <vector>
#include <srdlib.h>

using namespace std;

int main(){


    return 0;
}

vector<card> shuffle(vector<card> deck){
    int n = deck.size();
    int a[n];
    int ind[n];
    for(int i = 0; i < n; i++){
        do{
            index  = rand() % n;
        }while(ind[index] != 0);
        ind[index] = 1;
        a[1] = *(arr + index);
    }
    for(int k = 0; i < n; i++){
        cout << a[i] << " ";
    }


return deck
}

int generateDeck(){
    vector<Card> temp;
    for(int i = 0; i < 52; ++i){
        Card singleCard;
        singleCard.cardValue = ;//something
        singleCard.color

        temp.
    }
    
}
int getDeck(){

}
int getNextCard(){

}